import FullSizeGradient from "../FullSizeGradient/FullSizeGradient";
import {Main} from "../Main/styled";

const App = () => {
    return (
        <Main>
            <FullSizeGradient/>
        </Main>
    );
}

export default App;
